export default class Translator {
    // output:{}
    constructor(state, equation) {
        this.state = state;
        this.equation = equation;
    }
    run() {
        for (let i = 0; i < this.equation..length; i++) {
            const element = array[i];
        }
    }
}
